package Backend;
//if the system gets bigger this means that each object will behave differently depending on the class that implements it
public interface Records {
    public String Info();
    public String getSearchKey();

}
